import { Scene } from "phaser";
import { dataManager } from "../dataManager/Store";

// export function setGlobalSoundSettings(scene: Scene) {
//   scene.sound.setVolume(dataManager.getStore.option.volume);
// }
